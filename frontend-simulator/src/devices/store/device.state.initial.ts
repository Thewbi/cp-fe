import {DeviceState} from './device.state';

export const initialDeviceState: DeviceState = {
  devices: undefined,
  templatesAssigned: false,
  cacheStatus: undefined,
  selectedHomeDevice: undefined,
};
